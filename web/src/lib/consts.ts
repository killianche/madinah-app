/**
 * Единые пороги для всей системы.
 * Если меняешь — проверь, что нет других мест в коде с тем же числом.
 */

/** Учитель может редактировать/удалять свои уроки только не старше N дней. */
export const TEACHER_EDIT_LIMIT_DAYS = 30;

/** Балансовые пороги. Семантика разная — поэтому разные константы. */
export const BALANCE = {
  /** Критически низкий: ≤ этого = «без баланса», в attention low_balance, красным в карточке. */
  CRITICAL: 0,
  /** Низкий: ≤ этого = ученик скоро упрётся в ноль; учителю показывается «нужна оплата». */
  WARNING: 3,
  /** Score «проблемные»: < этого баланса добавляет к score композитной оценки. */
  PROBLEMS_SCORE_THRESHOLD: 5,
} as const;

/** Дни без проведённого урока — пороги «серой зоны» и stale. */
export const STALE = {
  /** Меньше — норма (≤ 7 дней между уроками — обычная неделя). */
  THRESHOLD_DAYS: 7,
  /** Больше — уже потеряны, не показываем во «Внимании». */
  CUTOFF_DAYS: 30,
  /** «Недавно бросили» — окно для возврата. */
  DROPPED_RECOVERY_DAYS: 10,
} as const;
