import { redirect } from "next/navigation";

/**
 * Глобальный 404 — редиректим на корень.
 * Сценарий: пользователь сохранил на главный экран iPhone роль-специфичную ссылку
 * (например /teacher), потом сменил аккаунт на куратора. Иконка тычет в /teacher,
 * который для куратора — 404. Чтобы flow не ломался, кидаем на /, оттуда уже
 * page.tsx редиректит на правильный home по роли.
 *
 * Этот же not-found срабатывает для опечаток в URL и удалённых маршрутов (/admin/*).
 */
export default function NotFound() {
  redirect("/");
}
